type {{.Class.Convert}} struct {
	*{{.Parent.Convert}}

	{{range .NamedChildren -}}
		{{.ID}} *{{.Class.Convert}}
	{{end}}
}

func New{{.Class.Convert}}({{.Parent.Constructor.Args.WithTypes}}) *{{.Class.Convert}} {
	{{- $desc := .Descendants -}}
	{{range $desc -}}
		{{.ID}} := {{.Class.Constructor}}({{.Class.Constructor.Args.Defaults}})
	{{end -}}
	parent := {{.Parent.Constructor}}({{.Parent.Constructor.Args}})

	{{range $desc -}}
		{{template "init.tmpl" .}}
	{{end -}}
	{{template "init.tmpl" .}}

	return &{{.Class.Convert}}{
		{{.Parent.Short}}: parent,

		{{range .NamedChildren -}}
			{{.ID}}: {{.ID}},
		{{end}}
	}
}
