{{- $id := or .ID "parent" -}}

{{range .Properties -}}
	{{$id}}.SetObjectProperty({{.Name | printf "%q"}}, {{.Value}})
{{end -}}
{{range $i, $c := .Children -}}
	{{- $cid := or .Object.ID (printf "%v%v" $id $i) -}}
	{{$id}}.{{$.Type.AddChild .Type $cid}}
{{end}}
